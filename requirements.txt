# Virtual Light - Raspberry Pi Optimized
# Tested on Raspberry Pi OS Bookworm (Python 3.11)

# Core - always required
# IMPORTANT: Do NOT install opencv-python-headless alongside opencv-python/openc-contrib.
# headless conflicts with GUI (cv2.imshow) and breaks imshow on Windows/Pi desktop.
# If you have headless installed: pip uninstall -y opencv-python-headless
opencv-python==4.11.0.86
numpy>=1.24.0,<2.0

# Lightweight tracking - PINNED to 0.10.14
# mediapipe >=0.10.30 removed mp.solutions (breaks tracker.py:22) -> symptoms: FaceDirection/BodyDirection DISABLED, always STOP, 0/25 calib stuck
# 0.10.14 is last version with solutions.face_mesh / pose / hands OR use tasks API migration
# Pi: install via piwheels: pip install --extra-index-url https://www.piwheels.org/simple mediapipe==0.10.14
mediapipe==0.10.14

# Optional - only for Pi Camera Module 3 / libcamera stack
# Install via: sudo apt install -y python3-picamera2
# picamera2 is NOT on pip for Pi; use apt

# Optional - for TFLite alternative (not required, heavier)
# tflite-runtime==2.14.0

# Dev / calibration
# matplotlib (for HSV calibration tool if needed)
